{
  "name": "hq-website-demand-test",
  "version": "0.0.1",
  "description": "website-demand-test",
  "main": "index.js",
  "scripts": {
    "build": "webpack",
    "update": "git pull",
    "checkout": "git checkout $VERSION",
    "build:prod": "yarn update && yarn checkout && yarn install && yarn build"
  },
  "devDependencies": {
    "dotenv-webpack": "^7.0.1",
    "webpack": "^5.4.0",
    "webpack-cli": "^4.2.0"
  }
}
